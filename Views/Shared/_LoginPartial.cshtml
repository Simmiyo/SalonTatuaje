@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

<ul class="nav navbar-nav navbar-right">
    <li class="nav-item">
        <a class="nav-link" href="@Url.Action("Index", "Manage", routeValues: null)">@("Salut, " + @User.Identity.GetUserName() + "!")</a>
        @*@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })*@
    </li>
    @*    <li>
            @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>*@
    <li class="nav-item"><a class="nav-link" href="javascript:document.getElementById('logoutForm').submit()">Ieși din cont</a></li>
</ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li class="nav-item">
            <a class="nav-link" href="@Url.Action("Register", "Account", routeValues: null)">Înregistrare</a>
            @*@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })*@
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@Url.Action("Login", "Account", routeValues: null)">Autentificare</a>
            @*@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })*@
        </li>
    </ul>
}
